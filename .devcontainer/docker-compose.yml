version: '3.8'

services:
  devcontainer:
    image: python:3.12
    volumes:
      - ../..:/workspaces:cached
    # network_mode: service:db
    # command: sleep infinity
    command: [ "python3 -m venv .venv", ". .venv/bin/activate", "pip install -r requirements.txt", "pymon scouting-app.py -p \"*.html\" -p \"*.css\" -p \"*.js\"" ]

#   db:
#     image: postgres:latest
#     restart: unless-stopped
#     volumes:
#       - postgres-data:/var/lib/postgresql/data
#     environment:
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_USER: postgres
#       POSTGRES_DB: postgres

# volumes:
#   postgres-data: