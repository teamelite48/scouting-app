services:

  devcontainer:
    image: python:3.12
    ports:
      - 5000:5000
    volumes:
      - ../:/app
    working_dir: /app
    entrypoint: ["bash", "-c", "pip install -r requirements.txt && python3 scouting-app.py"]

#   db:
#     image: postgres:latest
#     restart: unless-stopped
#     volumes:
#       - postgres-data:/var/lib/postgresql/data
#     environment:
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_USER: postgres
#       POSTGRES_DB: postgres

# volumes:
#   postgres-data:
